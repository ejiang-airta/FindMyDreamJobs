# .gitlab-ci.yml

stages:
  - test
  - deploy

variables:
  PYTHONPATH: "$CI_PROJECT_DIR/backend"

before_script:
  - echo "Running CI pipeline..."

# -------------------
# 🔍 Backend Tests
# -------------------
backend_tests:
  stage: test
  image: python:3.11
  script:
    - cd backend
    - pip install -r requirements.txt
    - pytest
  only:
    - dev
  artifacts:
    when: always
    paths:
      - backend/test-results/
    expire_in: 1 week

# -------------------
# 🧪 Frontend Tests (UI)
# -------------------
frontend_tests:
  stage: test
  image: mcr.microsoft.com/playwright:v1.39.0-focal
  script:
    - echo "Running frontend tests..."
    - pwd
    - cd frontend
    - npm install
    - npx playwright install
    - mkdir -p test-results  # Ensure the folder exists before collecting artifacts
    - echo "Testing starts"
    - npx playwright test login.spec.ts app_flow.spec.ts --output=test-results
    - echo "Test completed, checking results"
    - ls -la test-results/
    - find test-results/ -type f | head -20
    
  only:
    - dev
  artifacts:  
    when: always
    paths:
      - frontend/test-results/
    expire_in: 1 week
# -------------------
# 🚀 Deploy test-results artifacts to Public folder:
# -------------------
deploy-artifacts:
  stage: deploy
  script:
    - mkdir -p public/test-results
    - mv frontend/test-results/* public/test-results/ || echo "No artifacts to move"  # Prevent errors if folder is empty

  artifacts:
    when: always
    paths:
      - public/  # GitLab Pages stores this folder
    expire_in: 30 days  # Keep artifacts for debugging
  only:
    - dev # Only deploy on dev branch

# -------------------
# 🚀 Auto merget the dev branch to main on success pipline runs:
# -------------------
auto_merge_to_main:
  stage: deploy
  image: curlimages/curl:latest
  script: |
    - echo "Running CI pipeline..."
    # Step 1: Try to get an existing open MR from dev → main
    EXISTING_MR=$(curl --silent --header "PRIVATE-TOKEN: $GITLAB_TOKEN" \
      "https://gitlab.com/api/v4/projects/$CI_PROJECT_ID/merge_requests?state=opened&source_branch=dev&target_branch=main")

    MR_IID=$(echo "$EXISTING_MR" | grep -o '"iid":[0-9]*' | grep -o '[0-9]*' | head -1)

    # Step 2: If no open MR exists, create one
    if [ -z "$MR_IID" ]; then
      echo "No existing MR found. Creating one..."
      RESPONSE=$(curl --silent --request POST \
        --header "PRIVATE-TOKEN: $GITLAB_TOKEN" \
        --data "source_branch=dev&target_branch=main&remove_source_branch=false&title=AutoMerge dev to main" \
        "https://gitlab.com/api/v4/projects/$CI_PROJECT_ID/merge_requests")

      echo "Create MR API Response: $RESPONSE"
      MR_IID=$(echo "$RESPONSE" | grep -o '"iid":[0-9]*' | grep -o '[0-9]*' | head -1)
    else
      echo "Found existing MR IID: $MR_IID"
    fi

    # Step 3: Merge the MR
    if [ -n "$MR_IID" ]; then
      echo "Merging MR IID $MR_IID..."
      curl --fail --request PUT \
        --header "PRIVATE-TOKEN: $GITLAB_TOKEN" \
        "https://gitlab.com/api/v4/projects/$CI_PROJECT_ID/merge_requests/$MR_IID/merge"
    else
      echo "❌ Failed to find or create a merge request"
      exit 1
    fi


  only:
    - dev
  when: on_success
